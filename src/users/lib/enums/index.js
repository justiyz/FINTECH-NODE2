import * as messages from './lib.enum.messages';
import * as status from './lib.enum.status';
import * as label from './lib.enum.labels';
import * as queries from './lib.enum.queries';

export default {
  ...messages,
  ...status,
  ...label,
  ...queries
};
